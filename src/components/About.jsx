import Reveal from "./Reveal";

function About() {
  return (
    <Reveal>
      <section
        id="about"
        className="py-20 px-4 md:px-10 bg-gray-100 dark:bg-gray-900 transition-colors duration-500"
      >

        {/* all your existing About code stays here */}

      </section>
    </Reveal>
  );
}

export default About;